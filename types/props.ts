import { DefaultTheme } from "styled-components";

export type ButtonProps = { disabled: boolean } & { theme: DefaultTheme };

export type SelectProps = { selected: boolean } & { theme: DefaultTheme };
